# Basement Remote Sticky firmware
#
# This file is designed to be consumed as a remote ESPHome Git package. Remote
# packages cannot perform !secret lookups, so the local Device Builder wrapper
# supplies credential substitutions from its local secrets.yaml file.
substitutions:
  device_name: basement-remote-sticky
  friendly_name: Basement Remote Sticky
  firmware_version: "0.2.6"

esphome:
  name: ${device_name}
  friendly_name: ${friendly_name}
  min_version: 2026.8.2
  project:
    name: citizenracer.basement_remote
    version: "${firmware_version}"
  on_boot:
    # Sticky's power latch can survive deep sleep/reset. Release any old holds
    # before normal GPIO setup, then explicitly assert the keep-alive rails.
    - priority: 900
      then:
        - lambda: |-
            gpio_deep_sleep_hold_dis();
            gpio_hold_dis(GPIO_NUM_42);
            gpio_hold_dis(GPIO_NUM_45);
            gpio_hold_dis(GPIO_NUM_46);
    - priority: 600
      then:
        - output.turn_on: pwr_hold
        - output.turn_on: pwr_lock
        - switch.turn_on: touch_power_enable
        - delay: 1s
        - component.update: epaper_display
    - priority: -100
      then:
        - logger.log: "Basement Remote firmware ${firmware_version} ready"
  on_shutdown:
    # Preserve the power-latch state across controlled resets. Deep sleep is
    # intentionally NOT enabled in Phase 1.
    then:
      - lambda: |-
          gpio_hold_en(GPIO_NUM_45);
          gpio_hold_en(GPIO_NUM_46);
          gpio_deep_sleep_hold_en();

esp32:
  board: esp32-s3-devkitc-1
  variant: ESP32S3
  flash_size: 32MB
  framework:
    type: esp-idf
    advanced:
      # ESPHome 2026.8 requires this for OTA on ESP32-S3 targets with 32 MB flash.
      enable_idf_experimental_features: true

psram:
  mode: octal
  speed: 80MHz

logger:
  level: DEBUG
  hardware_uart: UART0

wifi:
  ssid: "${wifi_ssid}"
  password: "${wifi_password}"
  ap:
    ssid: "Basement Remote Fallback"
    password: "${fallback_ap_password}"

captive_portal:

api:
  encryption:
    key: "${basement_remote_api_encryption_key}"
  reboot_timeout: 0s

ota:
  - platform: esphome
    password: "${ota_password}"

# E-paper and microSD share SCK/MOSI on Sticky. Phase 1 only uses the display.
spi:
  id: display_spi
  clk_pin: GPIO13
  mosi_pin: GPIO14

# Seeed's current Sticky hardware documentation specifies touch SCL=GPIO2,
# SDA=GPIO3. This is intentionally opposite the pin ordering in the original
# project notes; both Seeed's docs and a known-working ESPHome Sticky config
# agree on this assignment.
i2c:
  - id: touch_i2c
    scl: GPIO2
    sda:
      number: GPIO3
      # GPIO3 is a strapping pin, but this is the board's documented touch SDA.
      ignore_strapping_warning: true
    frequency: 100kHz
    scan: true

output:
  - platform: gpio
    id: pwr_hold
    pin:
      number: GPIO45
      ignore_strapping_warning: true

  - platform: gpio
    id: pwr_lock
    pin:
      number: GPIO46
      ignore_strapping_warning: true

switch:
  - platform: gpio
    id: touch_power_enable
    internal: true
    restore_mode: ALWAYS_ON
    setup_priority: 1100
    pin: GPIO42

binary_sensor:
  - platform: gpio
    id: button_ai_power
    name: "AI Power Button"
    pin:
      number: GPIO4
      mode:
        input: true
        pullup: true
      inverted: true
    filters:
      - delayed_on: 10ms
      - delayed_off: 10ms
    on_press:
      - logger.log: "AI / Power button pressed"

  - platform: gpio
    id: button_up
    name: "Volume Up Button"
    pin:
      number: GPIO5
      mode:
        input: true
        pullup: true
      inverted: true
    filters:
      - delayed_on: 10ms
      - delayed_off: 10ms
    on_press:
      - logger.log: "Physical volume up button pressed"
      - script.execute: apple_tv_volume_up
      - delay: 500ms
      - while:
          condition:
            binary_sensor.is_on: button_up
          then:
            - script.execute: apple_tv_volume_up
            - delay: 175ms

  - platform: gpio
    id: button_down
    name: "Volume Down Button"
    pin:
      number: GPIO6
      mode:
        input: true
        pullup: true
      inverted: true
    filters:
      - delayed_on: 10ms
      - delayed_off: 10ms
    on_press:
      - logger.log: "Physical volume down button pressed"
      - script.execute: apple_tv_volume_down
      - delay: 500ms
      - while:
          condition:
            binary_sensor.is_on: button_down
          then:
            - script.execute: apple_tv_volume_down
            - delay: 175ms


globals:
  - id: touch_active
    type: bool
    restore_value: false
    initial_value: "false"

  - id: touch_start_x
    type: int
    restore_value: false
    initial_value: "-1"

  - id: touch_start_y
    type: int
    restore_value: false
    initial_value: "-1"

  - id: touch_started_ms
    type: uint32_t
    restore_value: false
    initial_value: "0"

  - id: touch_last_repeat_ms
    type: uint32_t
    restore_value: false
    initial_value: "0"

  # 0 = not repeatable; 1..5 = up/down/left/right/select.
  - id: touch_repeat_command
    type: int
    restore_value: false
    initial_value: "0"

script:
  # Remote commands match the Apple TV defaults used by the working
  # custom:universal-remote-card dashboard.
  - id: apple_tv_wakeup
    mode: restart
    then:
      - logger.log: "Apple TV command: wakeup"
      - homeassistant.action:
          action: remote.send_command
          data:
            entity_id: remote.basement_apple_tv
            command: wakeup

  - id: apple_tv_suspend
    mode: restart
    then:
      - logger.log: "Apple TV command: suspend"
      - homeassistant.action:
          action: remote.send_command
          data:
            entity_id: remote.basement_apple_tv
            command: suspend

  - id: apple_tv_menu
    mode: restart
    then:
      - logger.log: "Apple TV command: menu"
      - homeassistant.action:
          action: remote.send_command
          data:
            entity_id: remote.basement_apple_tv
            command: menu

  - id: apple_tv_home
    mode: restart
    then:
      - logger.log: "Apple TV command: home"
      - homeassistant.action:
          action: remote.send_command
          data:
            entity_id: remote.basement_apple_tv
            command: home

  - id: apple_tv_skip_backward
    mode: restart
    then:
      - logger.log: "Apple TV command: skip_backward"
      - homeassistant.action:
          action: remote.send_command
          data:
            entity_id: remote.basement_apple_tv
            command: skip_backward

  - id: apple_tv_play
    mode: restart
    then:
      - logger.log: "Apple TV command: play"
      - homeassistant.action:
          action: remote.send_command
          data:
            entity_id: remote.basement_apple_tv
            command: play

  - id: apple_tv_pause
    mode: restart
    then:
      - logger.log: "Apple TV command: pause"
      - homeassistant.action:
          action: remote.send_command
          data:
            entity_id: remote.basement_apple_tv
            command: pause

  - id: apple_tv_skip_forward
    mode: restart
    then:
      - logger.log: "Apple TV command: skip_forward"
      - homeassistant.action:
          action: remote.send_command
          data:
            entity_id: remote.basement_apple_tv
            command: skip_forward

  - id: apple_tv_up
    mode: restart
    then:
      - logger.log: "Apple TV command: up"
      - homeassistant.action:
          action: remote.send_command
          data:
            entity_id: remote.basement_apple_tv
            command: up

  - id: apple_tv_down
    mode: restart
    then:
      - logger.log: "Apple TV command: down"
      - homeassistant.action:
          action: remote.send_command
          data:
            entity_id: remote.basement_apple_tv
            command: down

  - id: apple_tv_left
    mode: restart
    then:
      - logger.log: "Apple TV command: left"
      - homeassistant.action:
          action: remote.send_command
          data:
            entity_id: remote.basement_apple_tv
            command: left

  - id: apple_tv_right
    mode: restart
    then:
      - logger.log: "Apple TV command: right"
      - homeassistant.action:
          action: remote.send_command
          data:
            entity_id: remote.basement_apple_tv
            command: right

  - id: apple_tv_select
    mode: restart
    then:
      - logger.log: "Apple TV command: select"
      - homeassistant.action:
          action: remote.send_command
          data:
            entity_id: remote.basement_apple_tv
            command: select

  - id: apple_tv_volume_down
    mode: restart
    then:
      - logger.log: "Apple TV command: volume_down"
      - homeassistant.action:
          action: remote.send_command
          data:
            entity_id: remote.basement_apple_tv
            command: volume_down

  - id: apple_tv_volume_up
    mode: restart
    then:
      - logger.log: "Apple TV command: volume_up"
      - homeassistant.action:
          action: remote.send_command
          data:
            entity_id: remote.basement_apple_tv
            command: volume_up

  - id: sonos_toggle_mute
    mode: restart
    then:
      - logger.log: "Sonos Arc command: toggle mute"
      - homeassistant.action:
          action: media_player.volume_mute
          data:
            entity_id: media_player.basement_sonos_arc
          data_template:
            is_volume_muted: >-
              {{ not state_attr('media_player.basement_sonos_arc',
                                'is_volume_muted') }}

sensor:
  - platform: wifi_signal
    name: "Wi-Fi Signal"
    update_interval: 60s

  - platform: uptime
    name: "Uptime"

  - platform: template
    id: last_touch_x
    name: "Last Touch X"
    unit_of_measurement: "px"
    accuracy_decimals: 0
    update_interval: never

  - platform: template
    id: last_touch_y
    name: "Last Touch Y"
    unit_of_measurement: "px"
    accuracy_decimals: 0
    update_interval: never

text_sensor:
  - platform: wifi_info
    ip_address:
      name: "IP Address"

font:
  # Material Design Icons v7.4.47, pinned so glyph codepoints cannot drift.
  # This is the only display font: the remote face intentionally has no text.
  - file:
      type: web
      url: https://raw.githubusercontent.com/Templarian/MaterialDesign-Webfont/v7.4.47/fonts/materialdesignicons-webfont.ttf
    id: font_mdi
    size: 60
    bpp: 1
    glyphsets: []
    glyphs:
      - "\U000F0425" # mdi-power
      - "\U000F004D" # mdi-arrow-left (Apple TV menu/back)
      - "\U000F02DC" # mdi-home
      - "\U000F04AB" # mdi-skip-backward
      - "\U000F040A" # mdi-play
      - "\U000F03E4" # mdi-pause
      - "\U000F04AC" # mdi-skip-forward
      - "\U000F0143" # mdi-chevron-up
      - "\U000F0140" # mdi-chevron-down
      - "\U000F0141" # mdi-chevron-left
      - "\U000F0142" # mdi-chevron-right
      - "\U000F0766" # mdi-circle-outline
      - "\U000F075F" # mdi-volume-mute

display:
  - platform: epaper_spi
    id: epaper_display
    model: Seeed-reTerminal-Sticky
    spi_id: display_spi
    rotation: 90
    update_interval: never
    full_update_every: 1
    lambda: |-
      const auto black = Color(0, 0, 0);
      const auto white = Color(255, 255, 255);
      const int w = it.get_width();
      const int h = it.get_height();

      it.fill(white);
      it.rectangle(0, 0, w, h, black);

      // Full-height, icon-only remote face. The control hierarchy follows
      // modern Apple TV / Google TV conventions: isolated power, dominant
      // D-pad, back/home directly below, transport controls, then mute.

      // Isolate the less-frequently used power control to prevent mis-taps.
      it.rectangle(360, 20, 100, 80, black);
      it.printf(410, 60, id(font_mdi), black, TextAlign::CENTER, "\U000F0425");

      // Primary navigation: largest and highest controls on the face.
      it.rectangle(150, 110, 180, 90, black);
      it.printf(240, 155, id(font_mdi), black, TextAlign::CENTER, "\U000F0143");
      it.rectangle(30, 215, 120, 110, black);
      it.printf(90, 270, id(font_mdi), black, TextAlign::CENTER, "\U000F0141");
      it.filled_rectangle(170, 215, 140, 110, black);
      it.printf(240, 270, id(font_mdi), white, TextAlign::CENTER, "\U000F0766");
      it.rectangle(330, 215, 120, 110, black);
      it.printf(390, 270, id(font_mdi), black, TextAlign::CENTER, "\U000F0142");
      it.rectangle(150, 340, 180, 90, black);
      it.printf(240, 385, id(font_mdi), black, TextAlign::CENTER, "\U000F0140");

      // Secondary navigation directly below the D-pad.
      it.rectangle(60, 455, 160, 90, black);
      it.printf(140, 500, id(font_mdi), black, TextAlign::CENTER, "\U000F004D");
      it.rectangle(260, 455, 160, 90, black);
      it.printf(340, 500, id(font_mdi), black, TextAlign::CENTER, "\U000F02DC");

      // Playback transport.
      it.rectangle(8, 565, 110, 90, black);
      it.printf(63, 610, id(font_mdi), black, TextAlign::CENTER, "\U000F04AB");
      it.rectangle(126, 565, 110, 90, black);
      it.printf(181, 610, id(font_mdi), black, TextAlign::CENTER, "\U000F040A");
      it.rectangle(244, 565, 110, 90, black);
      it.printf(299, 610, id(font_mdi), black, TextAlign::CENTER, "\U000F03E4");
      it.rectangle(362, 565, 110, 90, black);
      it.printf(417, 610, id(font_mdi), black, TextAlign::CENTER, "\U000F04AC");

      // Volume is handled by the two physical side buttons.
      it.filled_rectangle(160, 685, 160, 100, black);
      it.printf(240, 735, id(font_mdi), white, TextAlign::CENTER, "\U000F075F");

touchscreen:
  - platform: gt911
    id: sticky_touch
    display: epaper_display
    i2c_id: touch_i2c
    interrupt_pin: GPIO21
    reset_pin: GPIO41
    # Physical calibration on the portrait UI shows that the GT911 native X
    # axis already matches portrait X. Native Y runs in the opposite direction,
    # so mirror Y only. Do not swap axes: doing so rotates every hit region.
    transform:
      mirror_y: true
    on_touch:
      then:
        - logger.log:
            format: "Touch x=%d y=%d"
            args: [touch.x, touch.y]
        - sensor.template.publish:
            id: last_touch_x
            state: !lambda return touch.x;
        - sensor.template.publish:
            id: last_touch_y
            state: !lambda return touch.y;
        - lambda: |-
            const int x = touch.x;
            const int y = touch.y;
            const uint32_t now = millis();

            id(touch_active) = true;
            id(touch_start_x) = x;
            id(touch_start_y) = y;
            id(touch_started_ms) = now;
            id(touch_last_repeat_ms) = now;
            id(touch_repeat_command) = 0;

            // Repeatable controls fire immediately, then the interval below
            // repeats them after a deliberate hold.
            if (x >= 150 && x < 330 && y >= 110 && y < 200) {
              id(touch_repeat_command) = 1;
              id(apple_tv_up).execute();
            } else if (x >= 150 && x < 330 && y >= 340 && y < 430) {
              id(touch_repeat_command) = 2;
              id(apple_tv_down).execute();
            } else if (x >= 30 && x < 150 && y >= 215 && y < 325) {
              id(touch_repeat_command) = 3;
              id(apple_tv_left).execute();
            } else if (x >= 330 && x < 450 && y >= 215 && y < 325) {
              id(touch_repeat_command) = 4;
              id(apple_tv_right).execute();
            } else if (x >= 170 && x < 310 && y >= 215 && y < 325) {
              id(touch_repeat_command) = 5;
              id(apple_tv_select).execute();
            }
    on_release:
      then:
        - lambda: |-
            if (!id(touch_active)) {
              return;
            }

            const int x = id(touch_start_x);
            const int y = id(touch_start_y);
            const uint32_t held_ms = millis() - id(touch_started_ms);

            // Repeatable controls already fired on touch.
            if (id(touch_repeat_command) == 0) {
              if (x >= 360 && x < 460 && y >= 20 && y < 100) {
                if (held_ms >= 800) {
                  id(apple_tv_suspend).execute();
                } else {
                  id(apple_tv_wakeup).execute();
                }
              } else if (x >= 60 && x < 220 && y >= 455 && y < 545) {
                id(apple_tv_menu).execute();
              } else if (x >= 260 && x < 420 && y >= 455 && y < 545) {
                id(apple_tv_home).execute();
              } else if (x >= 8 && x < 118 && y >= 565 && y < 655) {
                id(apple_tv_skip_backward).execute();
              } else if (x >= 126 && x < 236 && y >= 565 && y < 655) {
                id(apple_tv_play).execute();
              } else if (x >= 244 && x < 354 && y >= 565 && y < 655) {
                id(apple_tv_pause).execute();
              } else if (x >= 362 && x < 472 && y >= 565 && y < 655) {
                id(apple_tv_skip_forward).execute();
              } else if (x >= 160 && x < 320 && y >= 685 && y < 785) {
                id(sonos_toggle_mute).execute();
              } else {
                ESP_LOGD("touch", "Touch was outside a control");
              }
            }

            id(touch_active) = false;
            id(touch_repeat_command) = 0;

interval:
  - interval: 50ms
    then:
      - lambda: |-
          if (!id(touch_active) || id(touch_repeat_command) == 0) {
            return;
          }

          const uint32_t now = millis();
          if ((now - id(touch_started_ms)) < 500 ||
              (now - id(touch_last_repeat_ms)) < 175) {
            return;
          }
          id(touch_last_repeat_ms) = now;

          switch (id(touch_repeat_command)) {
            case 1: id(apple_tv_up).execute(); break;
            case 2: id(apple_tv_down).execute(); break;
            case 3: id(apple_tv_left).execute(); break;
            case 4: id(apple_tv_right).execute(); break;
            case 5: id(apple_tv_select).execute(); break;
            default: break;
          }

button:
  - platform: template
    name: "Refresh E-Paper"
    entity_category: diagnostic
    on_press:
      - component.update: epaper_display
